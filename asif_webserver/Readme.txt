Runnning the script would return correlation response image containing detection of all nuclei for test image,

Procedure:

Run Kernal_RBF_Interface.py file for RBF Kernel FIlter and Linears_MOSSE_Interface.py for linear one:
	Setting path to folder containing all images (inside python script)
	Also select test image by mentioning its number stored in variabel j (inside python script)
	All Images would be  used in trainign except test images (that is 99 images are used in training)